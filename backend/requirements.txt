# FastAPI & Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# LangGraph & LangChain
langgraph>=1.0.0  # LangGraph 1.0+ required for interrupt, checkpointer, streaming features
# langchain-community 제거: 코드에서 사용하지 않으며 무거운 패키지로 빌드 타임아웃 발생

# OpenAI
openai>=1.0.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
alembic==1.12.1

# Data Validation (Python 3.13 compatible)
pydantic>=2.9.0
pydantic-settings>=2.5.0

# HTTP Client
httpx==0.25.2
requests==2.31.0

# Web Scraping
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Environment
python-dotenv==1.0.0

# Utilities
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4

# Search API (현재 사용하지 않음 - 초보자 모드는 크롤러만 사용)
# tavily-python>=0.3.0
